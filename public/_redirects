# RSS feed — preserve subscriber links from WordPress /feed/ URL
/feed/  /rss.xml  301
/feed   /rss.xml  301

# Off-topic post removed — redirect to homepage
/claude-pricing-explained/  /  301

# WordPress category archive URLs (wp uses /category/slug/, Astro uses /category/slug/)
# Already matching — no redirect needed

# www → apex redirect: cannot be done in _redirects (Cloudflare Pages
# does not support host-matching here). Set up via Cloudflare dashboard:
# Rules → Redirect Rules → "If hostname equals www.thedatagovernor.com,
# then dynamic 301 to concat('https://thedatagovernor.com', http.request.uri.path)"
